import React, { Component } from 'react';
import './index.css';

export class Post extends Component {
    render() {
        return (
            <h1>Post</h1>
        );
    };
};

export default Post;